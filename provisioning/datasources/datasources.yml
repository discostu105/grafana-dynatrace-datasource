apiVersion: 1

datasources:
  - name: 'dynatracegrail'
    type: 'discostu105-dynatracegrail-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    # Auth is supplied via DT_TENANT_URL and DT_TOKEN env vars on the plugin
    # process; no jsonData/secureJsonData fields needed in v0.
